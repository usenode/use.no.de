{
  "name": "use.no.de",
  "version": "0.0.0",
  "description": "The use.no.de website",
  "main": "server.js",
  "dependencies": {
        "micro":       ">=0.5.3",
        "proton":      ">=0.5.0",
        "spectrum":    ">=0.4.1",
        "promised-io": ">=0.3.0",
        "jsgi":        ">=0.2.5"
    },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/usenode/use.no.de.git"
  },
  "keywords": [
    "usenode"
  ],
  "author": "",
  "engines": {
    "node": ">=0.6"
  }
}
